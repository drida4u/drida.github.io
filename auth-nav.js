// ============================================================
// AUTH NAV — deprecated, kept as a harmless no-op.
// ------------------------------------------------------------
// Previously toggled the nav between "Login" and "Hi, Name / My
// Courses / Log Out" based on a Firebase login saved to
// localStorage. Firebase has been removed sitewide, so the nav
// link is now a plain static "Login" pointing straight to the
// WordPress dashboard (which shows its own login form or the
// real dashboard depending on WP's own session state).
//
// This file intentionally does nothing now. Safe to delete once
// you're confident nothing else references it.
// ============================================================
